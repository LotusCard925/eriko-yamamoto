{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    { "src": "dist/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
